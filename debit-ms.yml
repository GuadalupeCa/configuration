server:
  port: 8083
spring:
  application:
    name: debit-ms
  data:
    mongodb:
      uri: mongodb://localhost:27017/debitDB

management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: always
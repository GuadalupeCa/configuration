server:
  port: 8082
spring:
  application:
    name: deposit-ms
  data:
    mongodb:
      uri: mongodb://localhost:27017/depositDB

management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: always